# Copyright (c) Facebook, Inc. and its affiliates.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Linux Gluten Build

on:
  push:
    branches:
      - "gluten-0530"
    paths:
      - "velox/**"
      - "!velox/docs/**"
      - "CMakeLists.txt"
      - "CMake/**"
      - "third_party/**"
      - ".github/workflows/linux-gluten-build.yml"

  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    paths:
      - "velox/**"
      - "!velox/docs/**"
      - "CMakeLists.txt"
      - "CMake/**"
      - "third_party/**"
      - ".github/workflows/linux-gluten-build.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.repository }}-${{ github.head_ref || github.sha }}
  cancel-in-progress: true

jobs:
  linux-gluten:
    name: Build Velox for Gluten
    runs-on: ubuntu-latest
    container: ghcr.io/lgbo-ustc/velox4j:latest
    credentials:
      username: ${{ secrets.GHCR_USERNAME }}
      password: ${{ secrets.GHCR_TOKEN }}
    defaults:
      run:
        shell: bash
    env:
      CCACHE_DIR: "${{ github.workspace }}/ccache"
      VELOX_DEPENDENCY_SOURCE: SYSTEM
      GTest_SOURCE: BUNDLED
      simdjson_SOURCE: BUNDLED
      xsimd_SOURCE: BUNDLED
      folly_SOURCE: BUNDLED
      geos_SOURCE: BUNDLED
      USE_CLANG: "false"
    steps:
      - uses: actions/checkout@v4

      - name: Fix git permissions
        # Usually actions/checkout does this but as we run in a container
        # it doesn't work
        run: git config --global --add safe.directory ${GITHUB_WORKSPACE}

      - name: Zero Ccache Statistics
        run: ccache -sz

      - name: Configure Build
        run: |
          EXTRA_CMAKE_FLAGS=(
            "-DCMAKE_BUILD_TYPE=Release"
            "-DCMAKE_PREFIX_PATH=/usr/local"
            "-DBoost_DIR=/usr/local/lib/cmake/Boost-1.84.0"
            "-DVELOX_MONO_LIBRARY=ON"
            "-DVELOX_BUILD_SHARED=ON"
            "-DVELOX_BUILD_STATIC=OFF"
            "-DVELOX_BUILD_TESTING=OFF"
            "-DVELOX_ENABLE_ARROW=ON"
            "-DVELOX_ENABLE_KAFKA_CONNECTOR=ON"
            "-DVELOX_ENABLE_CCACHE=ON"
          )

          env xsimd_SOURCE=BUNDLED folly_SOURCE=BUNDLED cmake -S . -B _build/velox4j-like -GNinja "${EXTRA_CMAKE_FLAGS[@]}"

      - name: Build Velox
        run: cmake --build _build/velox4j-like --target velox -j$(nproc)

      - name: Ccache after
        run: ccache -s
